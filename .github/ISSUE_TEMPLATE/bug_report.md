---
name: Bug report
about: Report a data or tool bug
labels: bug
---

**Describe the bug**
A clear description.

**Reproduce**
Steps: `python scripts/build.py --check`, `python -m pytest`, `python scripts/bsd.py --halte ...`

**Expected**
What should happen (e.g., monotone violation, missing halte).

**Data**
Route/stop/trip IDs, `build_meta.json` counts.

**Context**
Branch, commit, `python --version`.
